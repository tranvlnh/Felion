# Workflows

Add CI after solution bootstrap. Minimum CI should restore, build and test the .NET 10 solution. Do not commit placeholder workflows that claim to validate projects that do not exist yet.
